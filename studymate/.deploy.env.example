# Ubuntu 生产环境启用 Caddy 公网入口和 Piston 代码运行服务。
COMPOSE_PROFILES=public,code-runner

# 只填写已备案域名，不带协议和路径，例如 studymate.example.com。
SITE_ADDRESS=studymate.example.com

# The independent interview service is exposed on its own host name.
# The backend's AI_INTERVIEW_PUBLIC_URL must use the same HTTPS URL.
AI_INTERVIEW_SITE_ADDRESS=interview.example.com

# 生产环境保持 80/443，供 Caddy 完成 ACME 验证和 HTTPS 服务。
HTTP_PORT=80
HTTPS_PORT=443

# 如果服务器无法访问 Docker Hub 或 GHCR，可改为可信镜像地址，
# 或填写已通过 `docker load` 导入的本地镜像名。
# CADDY_IMAGE=caddy:2-alpine
# PISTON_IMAGE=ghcr.io/engineer-man/piston:latest
# PYTHON_IMAGE=python:3.11-slim
# NODE_IMAGE=node:20-alpine
# NGINX_IMAGE=nginx:alpine
