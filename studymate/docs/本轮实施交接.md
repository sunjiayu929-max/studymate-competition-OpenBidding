# 2026-07-29 实施交接

本文只记录当前批次的实现边界，供后续任务继续，不包含账号、Key 或部署信息。

> 第二阶段已继续完成真实讲解时间轴、PPT 受控模型接口、私有知识后台任务和数据健康可见性；最新边界与剩余项见 [第二阶段实施交接](第二阶段实施交接.md)。下方“后续任务”保留第一阶段结束时的原始交接语境。

## 已形成完整闭环

- 登录后统一左侧应用壳；今日学习、学习资源工坊常驻，其余功能按需求分组，支持折叠、移动端抽屉和沉浸页。
- 首页增加全屏深色学习宇宙，节点进入真实功能，滚动后过渡到原暖白学习桌面，并提供返回宇宙入口与减少动态支持。
- 学习资源工坊命名已同步主要导航、页面、导览和文案。
- 私有知识库完成创建/管理、常见资料上传、解析与向量进度、课程绑定、来源页码、用户隔离和库内检索测试；助教可选绑定当前私有库。
- AI 助教与实时语音共享 Qwen、DeepSeek、MiMo 选择，服务端只返回配置状态，不返回凭据；图片问答不会静默换模型。
- 实时语音按 runId 隔离每轮 TTS，修复旧播放回调污染下一轮；支持播放中打断、已授权后的 ASR 限次自动重连和手动重试复位。
- PPT 生成已完成可验证 MVP：大纲确认、页面预览、文字编辑、拖动/按钮排序、单页重写、真实可编辑 `.pptx` 导出。
- 实时学习报告进入即根据已有数据呈现，并按轮询刷新真实数据；AI 阶段总结仅在里程碑自动生成或由用户手动重生成。
- 修复播放器普通模式黑色空区、介绍页太阳系重叠、退出后 `/events/batch` 401 竞态；正式 landing 与 preview 保持同步。
- 全局与语音舞台数字人使用透明素材，可拖动且区分点击，人物放大并增加呼吸、眨眼、微重心与连续状态动画。

## 后续任务应继续完成

- PPT 当前“单页重写”和初始大纲使用本地确定性策略，未调用模型；后续可接入已选模型、增加模板/图片/图表编辑和更完整的版式编辑器。现有导出不是假按钮。
- 实时语音自动测试只能覆盖 Mock/状态逻辑；仍需在测试密钥环境人工完成连续 5 轮、真实播放中打断、断网重连、文字/语音切换与不同浏览器音频策略。
- 私有知识库当前上传处理在请求内完成，适合 MVP；大文件生产化应改为后台任务、持久进度事件、OCR 和失败重试。`.ppt`/`.doc` 明确要求先转换为新版格式。
- 学习宇宙已包含轨道、真实入口节点、分层鼠标视差、飞船/流星和减少动态；“实时节点”当前来自进入首页时拉取的画像、笔记、测验、报告与工作台状态，后续可接服务端推送。
- 本批次没有建设开放平台、API Token 或开发者调试台，也没有启动/调用 Piston 服务。
