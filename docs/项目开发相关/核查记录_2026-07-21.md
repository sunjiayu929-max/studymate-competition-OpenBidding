# StudyMate 工作区路径迁移与 ESLint 收敛核查记录（2026-07-21）

本记录对应 2026-07-21 的工作区路径整理、Codex 会话兼容处理，以及前端 ESLint 规则收敛。不涉及密钥轮换，不修改 Docker 数据卷与接口契约。

## 1. 路径迁移

| 检查项 | 结果 |
| --- | --- |
| 规范路径 | 本仓库根目录 |
| 迁移方式 | 整目录 `mv`（含 `.git`、本地状态与未提交改动） |
| 兼容路径 | 会话 cwd 迁移后，旧路径软链接已删除 |
| 外层目录 | `Competition/Software Cup/` 下并列 `StudyMate` 与 `StudyMate-Doc` |
| Git 仓库 | 迁移后仍可在规范路径识别，远程与提交历史未因路径移动而丢失 |

## 2. Codex 对话兼容

| 检查项 | 结果 |
| --- | --- |
| 会话存储位置 | `~/.codex/sessions/`、`session_index.jsonl`、`history.jsonl`（不在项目目录内） |
| 旧会话 cwd | 历史会话元数据仍记录旧路径 `compitition` |
| 兼容策略 | 已将 `threads.cwd` 与会话文件中的旧绝对路径改为规范路径；软链接随后删除 |
| 注意 | 后续请只使用规范路径打开项目与 resume 会话 |

## 3. ESLint 收敛

| 检查项 | 结果 |
| --- | --- |
| 根因 | `eslint-plugin-react-hooks` 的 `flat.recommended` 包含约 16 条规则，其中多数为 React Compiler 约束 |
| 调整 | 不再继承 `reactHooks.configs.flat.recommended`；只显式启用 `rules-of-hooks`（error）与 `exhaustive-deps`（warn） |
| 文档 | 已同步 [`../../studymate/frontend/README.md`](../../studymate/frontend/README.md) 与 [`../../studymate/docs/开发与验收指南.md`](../../studymate/docs/开发与验收指南.md) |
| 附带修复 | `audioLevelMeter.ts` 中未使用参数 `_blob` 改为可选并 `void`，消除 `@typescript-eslint/no-unused-vars` |

## 4. 文档与规则

| 检查项 | 结果 |
| --- | --- |
| `CLAUDE.md` / `AGENTS.md` | 更新为规范路径 + 兼容软链接说明；`AGENTS.md` 仍指向 `CLAUDE.md` |
| 根 `README.md` | 增加本机路径说明 |
| `plan.md` | 路径迁移标为已完成；P2 改为保留软链接的维护提醒 |
| 索引 | `docs/README.md` 与 `docs/项目开发相关/README.md` 已挂接本记录 |

## 5. 未改动范围

- 不撤销或轮换任何 API Key / SMTP / 会话签名密钥。
- 不删除 Docker 数据卷，不覆盖生产或本机运行库。
- 不改写赛事原文、官方模板与外部参考 PDF/DOCX/HTML 正文。
- 用户工作树中与语音讲师头像相关的未提交改动予以保留，不回退覆盖。

## 6. 实测结果

| 检查项 | 结果 |
| --- | --- |
| 工作区结构检查 | `PYTHONDONTWRITEBYTECODE=1 python scripts/check_workspace_structure.py` 通过：根目录 8 项、Markdown 19 个、本地链接 51 条、Git 跟踪 377 个 |
| 前端 ESLint | `npm run lint` 通过，0 错误 0 警告 |
| `AGENTS.md` | 仍为指向 `CLAUDE.md` 的符号链接 |
| 兼容软链接 | 旧路径 `compitition` → 本仓库根目录，解析正确 |

## 7. Codex 会话 cwd 迁移

| 检查项 | 结果 |
| --- | --- |
| 备份 | 迁移成功后已删除（`~/.codex/backups/cwd-migrate-*`） |
| `threads.cwd` | 33 条旧路径全部改为规范路径；旧路径剩余 0；规范路径共 46 条 |
| 会话 rollout | 改写 53 个文件中的旧绝对路径；会话头中旧路径剩余 0 |
| `history.jsonl` | 用户消息正文中的旧路径字符串保留不动（仅历史文本） |
| 兼容软链接 | 已删除 |

## 8. 删除兼容软链接

会话 cwd 迁移与恢复验证成功后，已删除：

```text
compitition  （旧路径符号链接）
```

仅删除符号链接，未触碰本仓库内容。此后统一使用本仓库根目录。

