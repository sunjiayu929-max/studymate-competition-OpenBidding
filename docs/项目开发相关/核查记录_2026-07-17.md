# StudyMate 内容与功能核查记录（2026-07-17）

本记录对应 2026-07-17 的工作树，用于说明本轮文档重组、种子库、构建和核心接口的实际核查结果。真实模型额度、生产 HTTPS 和云端防火墙仍需在目标服务器上单独验证。

## 1. 文档与结构

| 检查项 | 结果 |
| --- | --- |
| Markdown 文档 | 检查 17 个现行 Markdown 文件，本地链接 0 个失效 |
| API 文档覆盖 | OpenAPI 中 66 个 `/api` 路径均能在 `接口说明.md` 中找到 |
| 前端路由覆盖 | `App.tsx` 中 19 个业务路由均能在前端 README 中找到 |
| 环境变量 | `backend/app/core/config.py` 与 `.env.example` 变量集合一致 |
| 协作规则 | `AGENTS.md` 正确指向 `CLAUDE.md` |
| 结构检查 | 自动检查根目录、应用根目录、前后端根目录、17 个 Markdown、本地链接和 Git 跟踪状态，结果通过 |
| 旧路径 | 现行文档不再链接旧版 `DEPLOY_UBUNTU.md`、`SECRETS.md` 和已拆分累计日志 |
| 父目录 | 保持 `compitition` 不变，避免影响 Codex CLI 当前会话和绝对路径 |
| 来源材料 | 赛题、参赛模板和外部参考 PDF/DOCX/HTML 已归类并建立索引，不改写权威原文 |

部署交付文件位于 `docs/交付文档/交付文件/`：项目部署文档 PDF 44 页，系统部署说明书 PDF 9 页；生成源码与素材位于相邻的 `生成脚本/`。DOCX/PDF 均抽取文本确认包含 7 组画像、300 个可视主题、人才呀公开资源、可解释岗位推荐和压缩种子库流程。

## 2. 前端

| 检查项 | 结果 |
| --- | --- |
| `npm run build` | 通过，TypeScript 与 Vite 生产构建成功 |
| Docker 前端镜像 | 构建通过，Nginx 配置检查通过 |
| 独立容器页面 | `/login` 返回 200，经容器网络代理 `/api/ping` 返回 200 |
| 截图脚本 | `node --check scripts/capture-page.mjs` 通过 |
| 全量 ESLint | 通过，前端源码与维护脚本无错误 |
| 浏览器核心回归 | 通过：建议 2-5 隔离回归均通过，覆盖示例填入、预取复用、专注模式、报告预览与热力图、未作答标签、资源底部翻页、RAG 分数、稳定外链、画像证据更新、人才呀精确匹配与 B站视频直达 |

浏览器回归使用独立 API mock，不修改运行数据库；同一主题在显式生成后只预取一次 `/concept/explain`，进入可视讲解页复用相同结果。

## 3. 后端、数据库与接口

| 检查项 | 结果 |
| --- | --- |
| Python 编译 | `app`、`scripts` 和文档生成脚本编译检查通过 |
| Docker 后端镜像 | 从依赖安装到应用、脚本、种子资源复制均构建通过 |
| 空数据卷启动 | 容器日志确认从 `studymate.db.gz` 解压运行库，健康检查通过 |
| 数据完整性 | 19 个演示账号、5 门课程、1709 个知识块；会话 0、验证码 0 |
| SQLite 校验 | `integrity_check=ok`，外键错误 0 |
| OpenAPI | 66 个 `/api` 路径，接口文档可生成 |

隔离容器中的接口冒烟结果：

- 健康检查、管理员登录、`/api/auth/me` 和退出登录通过；退出后会话失效。
- Cookie 在本地 HTTP 环境包含 `HttpOnly`、`SameSite=lax` 和有效期；生产环境仍需确认 `Secure`。
- 课程接口返回 5 门课程；RAG 返回 1709 个知识块且均已向量化，检索引擎标识为 BM25 + Vector RRF。
- 管理员可访问测试与埋点统计；普通学生访问对应接口返回 403。
- 人才呀课程接口本次返回 `live` 数据 2 条；岗位推荐返回 `live` 数据 2 条，并包含匹配分数与差距说明。
- 画像、报告、错题和外部资源自动化测试共 27 项：覆盖字段级画像清洗、历史项目证据、预览与安全写回、未作答历史兼容、主题×难度聚合、完成率与覆盖率、RRF 相对匹配度、外部资源相关性、阅读直链、编号账号幂等创建和 Python 沙箱预处理。
- 阅读解析接口每批最多处理 12 项，验证 arXiv/Crossref、豆瓣图书、CSDN 和掘金真实详情页；解析失败不伪造直链，前端保留稳定搜索入口。
- “CPU 取指执行周期”实时人才呀检索返回 0 个知识点级卡片，课程级“计算机硬件基础”等泛化结果不再展示。
- “原码反码补码”无人才呀知识点级课程时不展示课程卡片；B站实时检索返回经过相关性过滤的真实 BV 视频页。
- Piston Python runtime 已补齐 `scikit-learn`，并在送入沙箱前限制 OpenBLAS/OMP 等数值库线程数；逻辑回归示例可正常输出准确率。
- 助教课程会话接口正常返回；未调用真实模型，避免产生不可控费用。

## 4. Compose 与可视主题

- 基础、`code-runner`、`public`、`extras` 四种 Compose 配置均解析通过。
- 前后端 Docker 镜像均构建通过。
- 可视主题源码统计为 90 个专属动画 + 210 个脚本主题，共 300 个；脚本主题分布为 36、58、58、58，与四门 408 课程补齐目标一致，叠加原有主题后五门课各 60 个。

## 5. 密钥与本地状态

- Git 跟踪文件中只发现 `.env.example`，未发现真实 `.env`。
- 本地 `backend/.env` 存在且属于预期的机器配置；核查过程未输出其内容。
- 历史密钥清单已移出工作区，完成服务商侧轮换后仍需删除临时副本。
- 本地运行数据库、备份、日志、构建产物和测试结果保持在忽略范围内。

## 6. 未关闭风险

1. 课程新增/删除以及 RAG 导入/清空当前只要求登录，正式环境应限制为系统管理员。
2. 带 `user_id` 的画像和评估接口需要继续审计资源所有权。
3. 数据库仍使用启动期轻量迁移，长期维护应引入 Alembic。
4. 真实 LLM、视觉、语音、SMTP、Piston runtime 和生产 HTTPS 需要在有额度、域名和服务器配置的环境中完成最终验收。
